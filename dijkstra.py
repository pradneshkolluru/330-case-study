import csv
import sys

# def makeGraph():


# def dijkstra(graph, source):
#     unvisited_nodes = graph.get_nodes()
#     shortest_path = {}
#     previous_nodes = {}
#     max_value = sys.maxsize
#     #this is like setting the values of each node to be infinity as the starting point
#     for node in unvisited_nodes:
#         shortest_path[node] = max_value
#     #since the starting node distance to itself is 0
#     shortest_path[source] = 0
#     while unvisited_nodes:

# def main():
#     dijkstra()

# if __name__ == "__main__":
#     main()
